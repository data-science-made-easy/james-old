YES         <- c("y", "Y", "yes", "Yes", "YES")
SETTINGS    <- list(sep = ",") # Separator in xlsx-fields
TAB_NAME    <- list(help = "help", meta = "meta")
META        <- list(tab = "tab", id = "id", type = "type", import = "import", series_type = "series_type", swap_xy = "swap_xy")
ARGS        <- list(add_if_duplicate = "add_if_duplicate")
CLASS       <- list(jdata = "JData")
SERIES_TYPE_BAR_NEXT <- "bar--"
SERIES_TYPE_BAR_TOP <- "bar="
SERIES_TYPE_BAR <- c(SERIES_TYPE_BAR_NEXT, SERIES_TYPE_BAR_TOP)